因特网协议栈的五个层次：
	
		应用层：网络应用程序及其应用层协议存留的地方。        报文
				包括: HTTP(Web文档的请求和传送)、
					SMTP(电子邮件的传输)
					FTP(文件传送)
					DNS(域名系统协议)
				
		运输层：提供了在应用程序端点之间传送应用层报文的服务。 报文段
				包括：TCP(面向连接的服务)
					 UDP(无连接的服务)
							
		
		网络层：负责将称为数据报的网络层分组从一台主句移动到另一台主机。  数据报
					IP(定义了数据报中的各个字段以及端系统和路由器如何作用于这些字段)
		
		链路层：在每个节点，网络层将数据报下传给链路层，链路层沿着路径将数据报传递给下一个节点，在该下个节点，链路层将数据报上传给网络层。 链路层分组成为帧
				链路层的任务是将整个帧从一个网络元素移动到邻近的网络元素
		

物理层：物理层的任务是将该帧中的一个一个比特从一个节点移动到下一个节点。
